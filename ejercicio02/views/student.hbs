<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <style>
    table { border-collapse: collapse; width: 50%; margin-top: 20px; }
    th, td { border: 1px solid black; padding: 8px; text-align: center; }
    .highlight { background-color: #d4edda; font-weight: bold; }
  </style>
</head>
<body>
  <h1>{{title}}</h1>

  <table>
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Nota</th>
      </tr>
    </thead>
    <tbody>
      {{#each students}}
        <tr class="{{#if this.passed}}highlight{{/if}}">
          <td>{{this.name}}</td>
          <td>{{this.grade}}</td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</body>
</html>

